import { defineConfig } from '@playwright/test';

// Standard Playwright config (removed lovable wrapper)
export default defineConfig({
  // Add your custom playwright configuration overrides here
});
